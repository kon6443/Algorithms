# To run this program on zsh, 
# use the following command below.

# First step,
# g++ lcm.cpp -o lcm.out

# After executing above command, you will see a new file named `lcm.out`.

# Second step,
# ./lcm.out


